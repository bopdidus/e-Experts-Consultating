import { SpeicalPipePipe } from './speical-pipe.pipe';

describe('SpeicalPipePipe', () => {
  it('create an instance', () => {
    const pipe = new SpeicalPipePipe();
    expect(pipe).toBeTruthy();
  });
});
